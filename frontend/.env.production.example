# Production frontend environment values; copied to .env.production by the deploy
# workflow before `npm run build`. Never commit real secrets here.
# Leave VITE_STRIPE_PUBLISHABLE_KEY unset (or remove the line) to keep the
# legacy card-collection form on the checkout page.
VITE_STRIPE_PUBLISHABLE_KEY=pk_live_change_me